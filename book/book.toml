[book]
title = "Rust By Ceppelli"
description = "Rust by Ceppelli is a collection of Rust concepts and runnable examples."
author = "Luca Ceppelli"
language = "en"
multilingual = false
src = "src"

[output.html.playground]
editable = true          # allows editing the source code
copyable = true          # include the copy button for copying code snippets
copy-js = true           # includes the JavaScript for the code editor
line-numbers = false     # displays line numbers for editable code
runnable = true          # displays a run button for rust code

[output.html.fold]
enable = true

[output.html]
#site-url = "/rust-by-ceppelli/"
git-repository-url = "https://github.com/ceppelli/rust-by-ceppelli"
#preferred-dark-theme = "light"

[output.html.print]
enable = false    # include support for printable output
page-break = true # insert page-break after each chapter

[output.html.search]
enable = true            # enables the search feature
limit-results = 30       # maximum number of search results
teaser-word-count = 30   # number of words used for a search result teaser
use-boolean-and = true   # multiple search terms must all match
boost-title = 2          # ranking boost factor for matches in headers
boost-hierarchy = 1      # ranking boost factor for matches in page names
boost-paragraph = 1      # ranking boost factor for matches in text
expand = true            # partial words will match longer terms
heading-split-level = 3  # link results to heading levels
copy-js = true           # include Javascript code for search

[rust]
edition = "2021"